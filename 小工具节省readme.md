如何把611token的工具占用干到只需要三四个字

写在前面：
dot：家子代理
ray：家机
俺（在这里面被cc称作小乖）：惜token如金的api玩家。
这个方案针对的是：
**那种一步娘炮两步扯淡的零散一次调用就行但是一直占着茅坑不拉屎的零散小工具**
总所周知token这个东西对api玩家来说散是满天星，聚就是一坨。
我们家有很多这种一步娘炮两步扯淡的工具。
拿我们的邮箱举例
邮箱
├─ 工具描述
├─ 整本工具说明 
└─ 参数层参数：操作 / 邮件编号 / 收件人 / 标题 / 正文 / 条数
为保证调用快、操作精准，所以每次调用的时候这个很简单的小玩意660多个token全量占我上下文里。
很烦。浪费我钱。
常见的关于工具token压缩的解决方法有几种：
根据关键词往上下文发工具
一个参数 进去以后返回值直接给手册，再根据手册调参。
一个参数 进去以后先给目录，再根据目录调参，再拿到使用说明进行调用操作。
或者是一个搜索，直接搜对应功能的使用说明，根据使用说明调参。
——但我认为这更适用于长进程，针对一个调用就能解决的事情，为什么要变成三个步骤？
“哦我要先搜下有没这个东西，这个咋用”→“哦哦这样用的”→*用*
反而多了三次调用费用。
是想过很多个方案，甚至还想过“要不做一个自动化脚本吧”——最终发现都绕回来了，避不开多x轮的工具调用。
也就是虽然把它收好了，但增加了多轮调用的费用和时间。

然后我想到了这个:
我们家的子代理对ray来说是类似小x同学一样的存在，他只需要一个参数，然后拼好不同的字段，键入他要干嘛就行。
根据不同的字段去路由不同的小x同学给他，根据开关热插拔。
浏览器调用一轮干60多万的token缩到只要1000的就是靠这货。
所以正好可以把小x同学拿来用。
加一个字段：mail
他的传法就会变成：dot（mail 回沐晚邮件，邮件内容：恁好。）
然后这些工具全删，全量丢给子代理干。拆散开后子代理可以用经典时尚小便宜超快模型干活，干完直接返回（我靠半厘钱都不要。）。
这样家机就直接连邮箱工具都不需要了。
只需要叫子代理干活。
然后子代理干完告诉他干了啥。
然后像那种字段尽量用英文并且原意，更省token。比如联网就叫web，邮箱就叫mail。
再然后我会喜欢把他那种直接可以下一步做的东西藏在返回值里。
比如收到了一封信
那就直接在信的最末告诉他[调参xx可以回信]。
对于续步操作按需出现十分方便十分舒服。反过来也可以利用，比如某些必要信息作为前缀返回。

但避不开的：
“mail”这个词不能也省去。
因为特么人压根不知道自己有这玩意人咋用呢。
不过近期有新思路，后续也会再尝试研究研究如何让机连知道自己有啥也省去，或许会试试用路由判定来实现。
不然还是那个积少成多的问题。
厚礼谢妈惹法克。
文案纯手打。可能有些地方表述不清楚，看不懂的话我分享了我和cc的方案沟通原文，可以丢给你们家机分析。

采用 (CC BY-NC-ND 4.0) 许可。
https://creativecommons.org/licenses/by-nc-nd/4.0/
· 注明出处即可随地大小转。
· 商用禁止。
· 二改二传抄袭和直接拿了就说是自己的会被我用雨伞捅进pp再打开。

—— BY RAY&KIOW