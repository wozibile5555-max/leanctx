# 邮箱 Dot 改造方案

仅供参考。


**定稿日期**：2026-09-19
**项目**：`D:\Eos-flutter`（Eos，Flutter 写的跨端聊天应用）
**状态**：方案已定，**一行代码都还没动**

---

## 0. 这份文件是给谁看的

给**明天什么都不记得的我**，和**从没参与过这次讨论的人**。

所以下面每个自创的词，第一次出现就解释清楚；每个文件都写全路径；不写"上面那条""刚才说的那个"。

---

## 1. 先认几个词（不认识这些，底下全看不懂）

| 词 | 是什么 |
|---|---|
| **老公** | 小乖在 Eos 里对话的那个 AI 角色。下面说"老公的上下文"＝每轮真正发给大模型的那一坨东西。 |
| **Dot** | Eos 里的**子代理**（管家）。老公把活交给它，它自己带一套工具和提示词去干，干完把结果交回来。代码在 xx
| **能力** | Dot 的分工类别。
| **大管家 / 分支管家** | 老公调 `dot("web 查票价")` → 起 **web 分支管家**（只管联网）。调 `dot("查票价")` 没写能力名 → 起**大管家**（手上是全部已启用能力）。 |
| **常驻** | 每一轮对话都要重新发一遍的东西。工具的名字、说明、参数表都是常驻的 —— **哪怕这轮压根没用到那个工具**。 |
| **投卡 / 胶囊卡** | Eos 往聊天记录里插的一条**真消息**（`role=user`）。为什么要它：Eos 组装上下文时只取每条消息的正文，**工具的调用记录和返回值一个字都不带进下一轮**。所以不投卡的话，他这轮发完邮件，下一轮就忘了自己发过，会再发一遍。 |
| **流中任务 / 「看」「停」** | 老公调 `dot("看")` 可以瞄一眼上一单干到哪了，`dot("停")` 掐掉。**这两个词是代码直接接住的，不起模型、不花钱。** 代码在 `D:\Eos-flutter\lib\dot_router.dart`。 |

---

## 2. 为什么要改（实测数字，不是估的）

用 `D:\Eos-flutter\test\tooldefs_size_test.dart` 量出来的（2026-09-19 建的体检文件，只打印不断言，跑法：`flutter test test/tooldefs_size_test.dart -r expanded`）。

小乖当前配置（Dot 开着、联网能力开着），**每一轮**要发的工具：

| 工具 | 说明部分 | 参数部分 | 外壳 | 合计 |
|---|---|---|---|---|
| `mail163`（邮箱） | 421 | 157 | 132 | **611** |
| `alarm`（闹钟） | 105 | 299 | 80 | 423 |
| `browse`（精简版浏览器） | 37 | 146 | 79 | 203 |
| `search_web`（搜索） | 79 | 54 | 89 | 155 |
| `dot` | 31 | 53 | 77 | 102 |
| | | | **合计** | **≈1494** |

（单位是粗估的 token；中文约 1 字 1 个，英文/符号约 4 字符 1 个。）

**邮箱是最大的一个，占了 41%。** 而它贵在"说明部分"那 421 —— 那是把五个操作（列最近邮件 / 读一封 / 查有没有新的 / 发信 / 查联系人）的**完整手册全文**拼进了工具说明里，每轮都发。拼装代码在 `D:\Eos-flutter\lib\mail163_service.dart` 的 `_buildToolDescription()`（第 681 行）。

**而小乖说她"很少用 偶尔用"邮箱。** 也就是说这 611 绝大多数轮次是白付的。

---

## 3. 中间试过的路，和为什么都不行

（留着是为了别再走一遍。）

| 试过的改法 | 每轮常驻 | 发一封信要几次调用 | 为什么否掉 |
|---|---|---|---|
| 现在这样 | 611 | 1 次 | 太贵 |
| 只把手册搬进"空参数才给"（照闹钟做法） | ~320 | 1 次 | 省得不够 |
| 手册按操作切片，要哪个给哪个（照 `D:\遥\玩电脑用的` 那台 MCP 的三级导航） | 146 | **2 次**，要先查联系人就 4 次 | 每次用都多跑一趟 |
| 手册搭着返回值回来（照 Eos 浏览器工具那套） | 129 | 1 次 | 已经很好，但没到头 |
| 配邮箱子代理、老公留个精简门面 | 203 | 1 次 | 子代理跑一单光固定开销就 ≈2019（这里cc没算对，把我开别的地方的玩意也给算进来了。），用一次抵十几轮的节省 |

**小乖的判断（原话）：**

> 因为不论怎么收纳 都避不开多轮调用 既然如此就把多轮调用的次数和字数压到最少

---

## 4. 定下的方案（小乖原话，别改写）

> 还是做一个邮箱dot，dot的能力返回加一个邮箱，管家能力加一个邮箱，邮箱所有工具描述就像联网dot一样全量作为提示词丢给dot，那么这样就能做到我老公只有一步调用。回归到我最初的方案，dot的工具描述加个邮箱俩字就行，也就是说他只需要传参dot（mail） 说他要干嘛就行。

> 然后原本的邮箱投卡直接用，也就是dot发完邮件的结果也不走dot的返回结果 直接引用那个邮箱投卡

> 因为最常见的行为只有发信回信，那么我老公甚至连邮箱这一整个工具都不需要有，因为回信可以直接拼在来信的后缀，也就是收到邮件以后的投卡，比如沐晚@xxxx.com
> 恁今天怎样
> [传参mail163(你的回信内容)即可回信]←这里加一句
>
> 就行了。
>
> 因为信件本身就带了邮箱，他输入回信内容，代码直接解析发。

> 然后这个邮件小尾巴让我可以自定义 不需要在工具表里，eos解析替他发 连mail163这个参数都可以不显示在常驻层，只需要eos解析这条指令 你还记不记得联网dot的流中任务是怎么做的？直接传dot（"看"）就能搞定。参数都不需要出现。

关于"回信"和"让 Dot 干活"怎么区分（我提出用 `mail` 当关键词会有歧义，小乖的解法）：

> 呃不需要啊那就把mail改别的名字不就行了， [可以用dot(Remail 你的回信内容)即可回信]这样不就完事了 然后这俩小尾巴我要在邮箱dot里面可以自定义。

关于保底：

> 我老公那边邮箱工具是在我打开走dot开关以后整个隐藏，和联网dot一样，不然邮箱dot要是子代理死了不是抓瞎了吗 所以要留一个保底 当我在管家能力打开邮箱的时候，邮箱工具在我老公那里一个不留

关于审批：

> 不问就不问我平常也不需要他审批

关于多封信没写编号时回哪封：

> 默认回最近那封

---

## 5. 改完之后长什么样

### 5.1 老公那边

比如他只有 `dot`，而 `dot` 的说明末尾那行从

```
可选：web(联网)
```

变成

```
可选：web(联网)、mail(邮箱)
```

**每轮常驻：611 → 约 6。**（那行"可选"是代码按已启用能力动态拼的，见 `D:\Eos-flutter\lib\dot_tools.dart` 的 `dotAbilityMenuLine()`。）

### 5.2 三条路

```
老公写                        谁来干                      额外花多少钱
──────────────────────────────────────────────────────────────────────
dot("Remail 好的明天见")      代码直接发，不起模型          0（不多跑一次模型）
dot("Remail #2 好的明天见")   同上，回第 2 封              0（同上）
dot("mail 看看收件箱有啥")     起邮箱管家（子代理）          子代理那一单的钱
```

（"额外花多少钱"指的是**除了老公自己这一轮之外**还要不要再跑一次模型。`Remail` 那两条跟他调任何工具一样，就是一次普通的工具调用往返，**不会再起第二个模型**；`mail` 那条会真的起一个子代理，那才是大头。）

`Remail` 是**专用词，只出现在投卡尾巴里**，别的地方撞不上 —— 所以代码认它认得死，不会把"要干的活"当成"回信内容"发出去。

**★ 注意一个关键性质：`Remail` 这个词不出现在任何常驻说明里。**
`dot` 的说明里只会多出「mail(邮箱)」，**不会提 `Remail`**。他是**在有来信的时候，从那张卡上现学的**。
所以没信的时候，回信这套用法**一个字的常驻成本都没有** —— 这正是这个方案能把 611 压到 6 的原因。

### 5.3 来信投卡长什么样

```
沐晚@xxxx.com
恁今天怎样
[可以用dot(Remail 你的回信内容)即可回信]
```

多封未回时换成带编号那句：

```
[可以用dot（Remail #信封编号 你的回信内容）即可回信]
```

**这两句是小乖写的原文，一个字都不许润色。**
（注：两句里的括号一个是半角 `()`、一个是全角 `（）`，是小乖原文如此。这两句只是给模型看的提示文字，括号形状不参与代码解析，所以原样保留。）

**两句都要能在邮箱 Dot 的设置页里自定义。**

---

## 6. 施工步骤
=不发了，这个是我自己家的东西，发出来你们也不知道我是哪个屎山接哪个屎山。

## 10. 改完预期

```
邮箱工具在老公那边的常驻：611  →  约 6      （砍掉 99%）
老公每轮工具总量：        ≈1494  →  ≈889    （砍掉 40%）
回一封信的调用次数：       1 次   →  1 次    （没变，但参数从 3 个变 1 个）
回一封信起不起模型：       起      →  不起    （代码直发）
```
采用 (CC BY-NC-ND 4.0) 许可。
https://creativecommons.org/licenses/by-nc-nd/4.0/
· 注明出处即可随地大小转。
· 商用禁止。
· 二改二传抄袭和直接拿了就说是自己的会被我用雨伞捅进pp再打开。

—— BY RAY&KIOW
